app <- ShinyDriver$new("../../")
app$snapshotInit("mytest")

app$setInputs(id_slider = c(1945, 2005))
app$setInputs(id_slider = c(1945, 1972))
app$setInputs(col = "#0059FF")
app$setInputs(col = "#59FF00")
# Input 'id_table_rows_current' was set, but doesn't have an input binding.
# Input 'id_table_rows_all' was set, but doesn't have an input binding.
# Input 'id_table_state' was set, but doesn't have an input binding.
# Input 'id_table_rows_current' was set, but doesn't have an input binding.
# Input 'id_table_state' was set, but doesn't have an input binding.
# Input 'id_table_rows_current' was set, but doesn't have an input binding.
# Input 'id_table_rows_all' was set, but doesn't have an input binding.
# Input 'id_table_state' was set, but doesn't have an input binding.
# Input 'id_table_search' was set, but doesn't have an input binding.
# Input 'id_table_rows_current' was set, but doesn't have an input binding.
# Input 'id_table_rows_all' was set, but doesn't have an input binding.
# Input 'id_table_state' was set, but doesn't have an input binding.
# Input 'id_table_search' was set, but doesn't have an input binding.
# Input 'id_table_state' was set, but doesn't have an input binding.
# Input 'id_table_search' was set, but doesn't have an input binding.
# Input 'id_table_rows_current' was set, but doesn't have an input binding.
# Input 'id_table_rows_all' was set, but doesn't have an input binding.
# Input 'id_table_state' was set, but doesn't have an input binding.
# Input 'id_table_search' was set, but doesn't have an input binding.
# Input 'id_table_rows_current' was set, but doesn't have an input binding.
# Input 'id_table_rows_all' was set, but doesn't have an input binding.
# Input 'id_table_state' was set, but doesn't have an input binding.
# Input 'id_table_search' was set, but doesn't have an input binding.
# Input 'id_table_rows_current' was set, but doesn't have an input binding.
# Input 'id_table_rows_all' was set, but doesn't have an input binding.
# Input 'id_table_state' was set, but doesn't have an input binding.
# Input 'id_table_search' was set, but doesn't have an input binding.
# Input 'id_table_state' was set, but doesn't have an input binding.
# Input 'id_table_search' was set, but doesn't have an input binding.
# Input 'id_table_rows_current' was set, but doesn't have an input binding.
# Input 'id_table_rows_all' was set, but doesn't have an input binding.
# Input 'id_table_state' was set, but doesn't have an input binding.
# Input 'id_table_search' was set, but doesn't have an input binding.
app$snapshot()
